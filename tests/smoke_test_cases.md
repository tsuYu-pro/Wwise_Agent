# Wwise Agent 冒烟测试用例

> **版本**: 0.1.0  
> **测试日期**:  
> **测试人员**:  
> **环境要求**: Windows 10/11, Python 3.10+, Wwise 2024.1 运行中（WAAPI 已启用）

**结果填写说明**: Pass / Fail / Block（环境不满足）/ Skip（跳过）  
**缺陷描述**: 失败时简要描述现象

---

## 一、启动与基本 UI

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 1.1 | 正常启动 | 双击 `launcher.py` 或命令行 `python launcher.py` | 窗口正常显示，标题为 "Wwise Agent"，无报错弹窗 | 窗口正确显示，但是命令行里有错误代码 | tests\1.1.md |
| 1.2 | 窗口尺寸 | 观察启动后窗口 | 最小尺寸 420×600，默认约 450×700，可正常拖拽缩放 | 没问题 | |
| 1.3 | 深色主题 | 观察界面整体配色 | 深邃蓝黑背景（#0a0a12 色调），文字清晰可读 | 没问题 | |
| 1.4 | 多次启动 | 关闭应用后重新启动 | 窗口位置、大小与上次关闭时一致（工作区恢复） | 没问题，但是对话没有历史记录，我不确定这个是否符合预期 | |
| 1.5 | 最大化恢复 | 最大化窗口 → 关闭 → 重新启动 | 启动后自动最大化 | 没问题 | |

---

## 二、顶部设置栏

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 2.1 | Provider 切换 | 依次选择 WLAI / DeepSeek / Ollama / OpenAI / GLM / Duojie | 切换后模型下拉框内容随之变化 | 没问题 | |
| 2.2 | 模型选择 | 选择某个 Provider → 点击模型下拉框 | 显示该 Provider 对应的模型列表，选中后生效 | 没问题 | |
| 2.3 | API Key 状态 | 配置了 Key → 观察 Key 状态指示器 | 显示 Key 前几位截断（如 `sk-Yf…W5jr`） | 没问题 | |
| 2.4 | API Key 缺失 | 切换到一个未配置 Key 的 Provider | 显示 "No Key" 提示 | 没问题 | |
| 2.5 | Ollama 状态 | 选择 Ollama Provider，本地 Ollama 未启动 | 显示 "Offline"；启动 Ollama 后显示 "Local" | 不关心 | |
| 2.6 | Web 开关 | 点击 Web 复选框开启/关闭 | 开关状态切换，影响后续对话是否联网搜索 | 没问题 | |
| 2.7 | Think 开关 | 点击 Think 复选框开启/关闭 | 开关状态切换，影响后续对话是否启用思维链 | 有点问题但不多 | tests\2.7.md |

---

## 三、溢出菜单（··· 按钮）

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 3.1 | 打开菜单 | 点击右上角 `···` 按钮 | 弹出菜单，包含 API Key / Clear Chat / Cache / Optimize / Update / Font / Language |没问题 | |
| 3.2 | 设置 API Key | 菜单 → API Key → 输入/修改 Key → 确认 | Key 保存成功，状态指示器更新 | 没问题 | |
| 3.3 | 清空对话 | 菜单 → Clear Chat | 当前对话历史清空，输入框可用 |  没问题 | |
| 3.4 | 缓存管理 | 菜单 → Cache | 缓存管理界面正常打开，显示缓存大小 | 没问题 | |
| 3.5 | Token 优化 | 菜单 → Optimize | 优化面板打开，显示优化选项 |  | |
| 3.6 | 检查更新 | 菜单 → Update | 连接 GitHub API 检查版本，提示"已是最新"或弹出更新对话框 | | |
| 3.7 | 字号调节 | 菜单 → Font → 拖动滑块到 120% | 界面所有文字即时放大；拖到 80% 则缩小 | | |
| 3.8 | 字号持久化 | 设置字号 120% → 重启应用 | 重启后字号仍然为 120% | | |
| 3.9 | 语言切换-英文 | 菜单 → Language → EN | 界面文案切换为英文 | | |
| 3.10 | 语言切换-中文 | 菜单 → Language → 中文 | 界面文案切换回中文 | | |
| 3.11 | 语言持久化 | 切换语言 → 重启应用 | 重启后语言设置保留 | | |

---

## 四、会话管理

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 4.1 | 新建会话 | 点击标签栏的 "+" 按钮 | 新增一个空白对话标签 | | |
| 4.2 | 多会话并行 | 新建 3 个会话，在不同会话中输入不同内容 | 切换标签时各会话内容独立保持 | | |
| 4.3 | 关闭会话 | 右键某个标签 → 关闭此对话 | 该标签关闭，其他标签不受影响 | | |
| 4.4 | 关闭其他 | 右键某个标签 → 关闭其他对话 | 仅保留当前标签，其余全部关闭 | | |
| 4.5 | 会话恢复 | 有多个会话 → 关闭应用 → 重新启动 | 所有会话标签和对话内容恢复 | | |
| 4.6 | 自动标题 | 发送一条消息并等待 AI 回复 | AI 回复后会话标签自动更新为有意义的标题 | | |

---

## 五、输入区域

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 5.1 | 发送消息 | 输入 "你好" → 按 Enter | 消息发送，输入框清空，AI 开始回复 | | |
| 5.2 | 换行 | 输入内容 → 按 Shift+Enter | 输入框内换行，不发送 | | |
| 5.3 | 停止生成 | AI 回复过程中 → 点击 Stop 按钮 | AI 停止回复，可以继续输入新消息 | | |
| 5.4 | 模式切换-Agent | 模式下拉框选择 "Agent" | 切换成功，后续对话使用全工具模式 | | |
| 5.5 | 模式切换-Ask | 模式下拉框选择 "Ask" | 切换成功，后续对话只使用只读查询工具 | | |
| 5.6 | 模式切换-Plan | 模式下拉框选择 "Plan" | 切换成功，AI 先生成计划再执行 | | |
| 5.7 | 确认模式开启 | 勾选 Cfm 复选框 | 后续修改性操作弹出预览确认卡片 | | |
| 5.8 | 图片拖拽 | 拖拽一张 PNG 图片到输入框 | 输入框上方出现图片预览缩略图 | | |
| 5.9 | 图片粘贴 | 复制一张图片 → Ctrl+V 粘贴到输入框 | 输入框上方出现图片预览缩略图 | | |
| 5.10 | 移除图片 | 添加图片后点击缩略图的删除按钮 | 图片预览移除 | | |
| 5.11 | 附件菜单-图片 | 点击 "+" 按钮 → Attach Image → 选择图片 | 图片添加成功，显示预览 | | |
| 5.12 | Token 统计 | 发送几条消息后，查看 Token 统计按钮 | 显示累计 Token 量和费用（如 "33.9K \| $0.01"） | | |
| 5.13 | Token 详情面板 | 点击 Token 统计按钮 | 弹出 Token Analytics Panel，显示每次调用的详细记录和柱状图 | | |
| 5.14 | 上下文统计 | 观察上下文百分比显示 | 显示格式如 "3.2% 6.4K/200K"，随对话增长 | | |

---

## 六、对话视图

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 6.1 | 用户消息气泡 | 发送一条文字消息 | 右对齐显示用户消息气泡 | | |
| 6.2 | AI 回复渲染 | 等待 AI 回复 | AI 回复支持 Markdown 渲染（标题、列表、代码块等） | | |
| 6.3 | 思考区折叠 | Think 开启时发送问题 → 观察回复 | 思考内容显示在可折叠区块中，可点击展开/收起 | | |
| 6.4 | 流式输出 | 发送消息后观察 AI 回复过程 | 文字逐步流式显示，而非一次性出现 | | |
| 6.5 | 渐变光带 | AI 回复过程中观察左侧 | 显示流动渐变极光动画效果 | | |
| 6.6 | 工具执行折叠 | Agent 模式下让 AI 执行工具 | 工具执行过程以可折叠区块显示 | | |
| 6.7 | 智能滚动 | 大量对话后手动上翻浏览 → AI 回复中 | 用户上翻时不强制滚动到底部 | | |
| 6.8 | 自动滚底 | 不手动上翻，AI 回复中 | 自动滚动到底部跟踪最新内容 | | |
| 6.9 | 图片消息 | 带图片发送消息 | 用户消息气泡中显示图片缩略图，可点击放大 | | |

---

## 七、Wwise 连接 & 查询类 Skills

> **前提**: Wwise 2024.1 正在运行，WAAPI 已启用（默认端口 8080）

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 7.1 | Skills 加载 | 启动应用，观察控制台 | 输出 "[Skills] 已加载 N 个 skill"，无 `No module named 'wwise_skills'` 错误 | | |
| 7.2 | 获取选中对象 | 在 Wwise 中选中一个对象 → 输入"帮我看看我选中了什么" | AI 调用 `get_selected_objects`，正确返回选中对象信息 | | |
| 7.3 | 项目结构概览 | 输入"展示一下项目结构" | AI 调用 `get_project_hierarchy`，返回顶层层次结构 | | |
| 7.4 | 搜索对象 | 输入"搜索名字包含 BGM 的对象" | AI 调用 `search_objects`，返回匹配结果 | | |
| 7.5 | 获取对象属性 | 输入"查看 Default Work Unit 的属性" | AI 调用 `get_object_properties`，返回属性详情 | | |
| 7.6 | Bus 拓扑 | 输入"展示 Bus 拓扑结构" | AI 调用 `get_bus_topology`，返回 Master-Mixer Bus 树 | | |
| 7.7 | Event 动作 | 输入"查看某个 Event 的 Action 详情"（替换为真实 Event 名） | AI 调用 `get_event_actions`，返回 Action 列表 | | |
| 7.8 | SoundBank 信息 | 输入"查看 SoundBank 信息" | AI 调用 `get_soundbank_info`，返回 Bank 列表 | | |
| 7.9 | RTPC 列表 | 输入"列出所有 Game Parameter" | AI 调用 `get_rtpc_list`，返回 RTPC/Game Parameter 列表 | | |
| 7.10 | Effect 链 | 输入"查看某个 Bus 的 Effect 链"（替换为真实 Bus 名） | AI 调用 `get_effect_chain`，返回 Effect 插件链 | | |
| 7.11 | Wwise 未连接 | 关闭 Wwise → 尝试查询操作 | 提示连接失败，给出排查建议，不崩溃 | | |

---

## 八、Wwise 修改类 Skills

> **前提**: Wwise 连接正常。建议在测试项目中操作，避免影响正式项目。

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 8.1 | 创建对象 | 输入"在 Default Work Unit 下创建一个 Sound SFX 叫 Test_Sound" | AI 调用 `create_object`，Wwise 中出现新对象 | | |
| 8.2 | 创建 Event | 输入"为 Test_Sound 创建一个播放 Event" | AI 调用 `create_event`，Wwise 中出现 Event | | |
| 8.3 | 设置属性 | 输入"把 Test_Sound 的音量设为 -6" | AI 调用 `set_property`，属性在 Wwise 中更新 | | |
| 8.4 | 分配 Bus | 输入"把 Test_Sound 路由到 Master Audio Bus" | AI 调用 `assign_bus`，Bus 路由更新 | | |
| 8.5 | 移动对象 | 输入"把 Test_Sound 移动到另一个 Work Unit"（指定路径） | AI 调用 `move_object`，对象位置变化 | | |
| 8.6 | 添加 Effect | 输入"给 Master Audio Bus 加一个 RoomVerb" | AI 调用 `add_effect`，Effect 出现 | | |
| 8.7 | 移除 Effect | 输入"移除刚才加的 RoomVerb" | AI 调用 `remove_effect`，Effect 清除 | | |
| 8.8 | RTPC 绑定 | 输入"创建一个 Game Parameter 并绑定到某个属性" | AI 调用 `set_rtpc_binding`，RTPC 曲线绑定成功 | | |
| 8.9 | 删除对象 | 输入"删除 Test_Sound" | AI 调用 `delete_object`，对象从 Wwise 移除 | | |
| 8.10 | 试听 Event | 输入"试听一下这个 Event" | AI 调用 `preview_event`，Wwise 播放音频 | | |
| 8.11 | 确认模式拦截 | 勾选 Cfm → 执行创建操作 | 弹出 WwisePreviewInline 预览卡片，显示参数，用户 Accept 后执行 | | |
| 8.12 | 确认模式取消 | Cfm 模式下 → 预览卡片点击 Cancel | 操作取消，不执行 | | |
| 8.13 | 批量操作 Undo | 执行多个修改后 → 点击 "Undo All" | 所有操作撤销 | | |
| 8.14 | 批量操作 Keep | 执行多个修改后 → 点击 "Keep All" | 所有操作保留 | | |

---

## 九、验证类 Skills

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 9.1 | 结构验证 | 输入"验证一下项目结构完整性" | AI 调用 `verify_structure`，返回验证报告 | | |
| 9.2 | Event 完整性 | 输入"检查所有 Event 是否完整" | AI 调用 `verify_event_completeness`，返回检查结果 | | |
| 9.3 | 原始 WAAPI | 输入"用 WAAPI 执行 ak.wwise.core.getInfo" | AI 调用 `execute_waapi`，返回 Wwise 版本等信息 | | |
| 9.4 | 黑名单拦截 | 尝试通过 execute_waapi 调用黑名单接口 | AI 拒绝执行，提示该操作在安全黑名单中 | | |

---

## 十、Agent 模式（全自主）

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 10.1 | 多步骤任务 | Agent 模式 → 输入"创建一个完整的脚步声系统：RandomContainer + 3个子Sound + Event" | AI 自动规划并依次调用多个工具完成，无需手动干预 | | |
| 10.2 | 工具链路径 | 观察 10.1 的执行过程 | AI 回复中以可折叠区块展示每次工具调用及结果 | | |
| 10.3 | 错误恢复 | Agent 执行中故意给出错误路径 | AI 能识别错误并尝试纠正（如重新搜索正确路径） | | |

---

## 十一、Ask 模式（只读）

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 11.1 | 只读查询 | Ask 模式 → 输入"帮我查一下项目里有什么 Event" | AI 只使用查询工具，成功返回信息 | | |
| 11.2 | 拒绝修改 | Ask 模式 → 输入"帮我创建一个新 Sound" | AI 不执行修改操作，提示切换到 Agent 模式 | | |

---

## 十二、Plan 模式

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 12.1 | 生成计划 | Plan 模式 → 输入"帮我重组项目结构" | AI 生成 Plan 卡片（含标题、阶段、步骤），不立即执行 | | |
| 12.2 | Plan 卡片展示 | 观察 Plan 卡片 | 流式显示 Plan 结构，含复杂度评估、阶段划分、步骤依赖 | | |
| 12.3 | 确认计划 | Plan 卡片 → 点击确认/接受 | AI 开始按计划逐步执行 | | |
| 12.4 | 拒绝计划 | Plan 卡片 → 点击拒绝 | 不执行，可以重新描述需求 | | |
| 12.5 | AI 提问 | Plan 模式下信息不足时 | AI 弹出 AskQuestionCard 向用户提问 | | |

---

## 十三、联网搜索 & 文档检索

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 13.1 | Web 搜索 | Web 开关打开 → 输入"Wwise 2024 有什么新功能" | AI 调用 `web_search`，结果中包含搜索来源 | | |
| 13.2 | 网页抓取 | 上一步的基础上，AI 需要深入某链接 | AI 调用 `fetch_webpage` 抓取网页内容 | | |
| 13.3 | Web 关闭 | 关闭 Web 开关 → 输入类似问题 | AI 不联网搜索，仅基于自身知识回答 | | |
| 13.4 | 本地文档搜索 | 输入"WAAPI 的 ak.wwise.core.object.create 怎么用" | AI 调用 `search_local_doc`，返回本地文档索引结果 | | |

---

## 十四、记忆系统

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 14.1 | 记忆积累 | 完成多次对话任务 | `cache/memory/agent_memory.db` 文件产生/增长 | | |
| 14.2 | 记忆召回 | 之前让 AI 做过某操作 → 再次提到类似需求 | AI 回复中体现之前的经验（如提到"之前我们做过…"） | | |
| 14.3 | 成长档案 | 完成若干任务后查看 `cache/memory/growth_profile.json` | 文件存在，包含个性特征数据（efficiency_bias, risk_tolerance 等） | | |

---

## 十五、训练数据导出

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 15.1 | 导出训练数据 | 有对话内容 → 点击 "+" → Export Train | 在 `trainData/` 目录生成 `.jsonl` 文件 | | |
| 15.2 | 文件格式 | 打开生成的 `.jsonl` 文件 | 每行一个 JSON，符合 OpenAI 微调格式（messages 数组） | | |

---

## 十六、Wwise 路径交互

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 16.1 | 路径链接 | AI 回复中包含 Wwise 对象路径 | 路径以绿色等宽字体显示，可点击 | | |
| 16.2 | 点击定位 | 点击 AI 回复中的 Wwise 路径 | Wwise 中自动选中/定位到该对象 | | |

---

## 十七、MCP Server

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 17.1 | MCP 启动 | 命令行执行 `python -m wwise_mcp.server` | 服务器正常启动，无报错 | | |
| 17.2 | 工具列表 | MCP 客户端（如 Claude Desktop）连接后请求工具列表 | 返回 22 个 Wwise 工具 | | |
| 17.3 | 工具调用 | 通过 MCP 客户端调用 `get_project_hierarchy` | 返回正确的 Wwise 项目结构 | | |

---

## 十八、异常 & 边界

| 编号 | 测试项 | 操作步骤 | 预期结果 | 结果 | 缺陷描述 |
|------|--------|---------|---------|------|---------|
| 18.1 | 空消息发送 | 输入框为空 → 点 Send 或按 Enter | 不发送，无异常 | | |
| 18.2 | 超长消息 | 输入数千字的长文本 → 发送 | 正常发送并显示，不截断不崩溃 | | |
| 18.3 | 快速连续发送 | 快速连续发送多条消息 | 不崩溃，按顺序处理或提示等待 | | |
| 18.4 | API Key 错误 | 填写无效 Key → 发送消息 | 提示认证失败，不崩溃 | | |
| 18.5 | 网络断开 | 断网 → 发送消息 | 提示网络错误，不崩溃 | | |
| 18.6 | Wwise 中途断开 | 对话中关闭 Wwise | 工具调用报连接错误，Agent 能优雅处理 | | |
| 18.7 | 长时间空闲 | 保持应用打开 30 分钟以上不操作 | 应用不假死，可继续正常操作 | | |

---

## 测试结果汇总

| 模块 | 总数 | Pass | Fail | Block | Skip |
|------|------|------|------|-------|------|
| 一、启动与基本 UI | 5 | | | | |
| 二、顶部设置栏 | 7 | | | | |
| 三、溢出菜单 | 11 | | | | |
| 四、会话管理 | 6 | | | | |
| 五、输入区域 | 14 | | | | |
| 六、对话视图 | 9 | | | | |
| 七、查询类 Skills | 11 | | | | |
| 八、修改类 Skills | 14 | | | | |
| 九、验证类 Skills | 4 | | | | |
| 十、Agent 模式 | 3 | | | | |
| 十一、Ask 模式 | 2 | | | | |
| 十二、Plan 模式 | 5 | | | | |
| 十三、联网搜索 | 4 | | | | |
| 十四、记忆系统 | 3 | | | | |
| 十五、训练数据导出 | 2 | | | | |
| 十六、路径交互 | 2 | | | | |
| 十七、MCP Server | 3 | | | | |
| 十八、异常 & 边界 | 7 | | | | |
| **合计** | **112** | | | | |
